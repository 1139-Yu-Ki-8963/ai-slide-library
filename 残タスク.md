# 公開フローの是正

## 設計

設計は `docs/公開フロー設計.md` にある。
現行フローで起きた6件の事象、6つの設計原則、A〜G のフロー、停止条件、実装対象、検証方法を記載している。

## 実装する項目

| 項目 | 対象 |
|---|---|
| Phase 順序を A〜G へ置き換え、AIレビューから公開停止の権限を外す | `.claude/skills/adding-catalog-slides/SKILL.md` |
| 削除の承認手順を Phase A として追加する | `.claude/skills/adding-catalog-slides/SKILL.md` |
| blocker の閾値を critical と fail に限定する | `aggregate-html-review.mjs` |
| 実描画検査を対象ごとの独立プロセスにする | `verify-html-runtime.mjs` |
| ワークフローを検査用と配信用に分割する | `.github/workflows/` |
| 規約の導出順序を前書きに明記する | `slide-design-system.md` |

## 着手方法

`Skill("managing-agent-configs")` を種別 skills で実行する。
`docs/公開フロー設計.md` の第5節を実装対象、第6節を完了判定に使う。
