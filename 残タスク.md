# 残タスク

## 登録・公開フローの構造欠陥の是正

2026-07-28 の新デザイン移行で顕在化した3点。
承認済みの成果物に対してレビューが6回の修正を要求し、作業が長時間化した。

### 欠陥1: 承認された成果物にレビューが拒否権を持っている

`adding-catalog-slides` の Phase 順序は、AIレビュー（Phase 8〜11）、コミット（Phase 15）、ユーザー提示・承認（Phase 16）の順になっている。
承認が最後にあるため、ユーザーが内容を承認した後にレビューが指摘を出し、修正を強いる。

あるべき姿は、ユーザー承認を AIレビューの上位に置くことである。
承認後に公開を止めてよいのは、機械検証（構造契約・表示破綻・公開安全）だけとする。
AIレビューは指摘を記録するが公開を止めない。

対象は `.claude/skills/adding-catalog-slides/SKILL.md` の Phase 順序。

### 欠陥2: 規約が実物から導かれていない

デザイン規約を実物より先に書き、後から来た実物との差分がすべて違反として扱われた。
緩和が6件必要になったが、6件とも規約が実物を知らなかっただけで、実物に問題はなかった。

あるべき姿は、承認された実物から規約を抽出して固定し、以後の新規スライドをそれで検査することである。
実物より先に規約を書かない。

対象は `slide-design-system.md` の運用手順と、型登録の手順。

### 欠陥3: 提案レベルの指摘が公開を止める

`aggregate-html-review.mjs` は `pass` 以外をすべて blocker として扱う。
低優先度の提案（`suggestion`）1件でコミットが止まった。

あるべき姿は、blocker の閾値を `critical` と `fail` に限定することである。
`hold` と `suggestion` は記録に残すが公開を止めない。

対象は `aggregate-html-review.mjs` の判定条件。

## 着手方法

`Skill("managing-agent-configs")` を種別 skills で実行し、上記3点を修正する。
