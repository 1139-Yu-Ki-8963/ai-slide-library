# 検査記録: 既存コード整備優先順位-変更起点

## 観点レビュー表の照合結果（2026-07-19）

| 観点キー | 判定 | 備考 |
|---|---|---|
| 文字切れ-全数目視 | PASS | 1280×720 実描画（`evidence/検証用スクリーンショット.png`）で全テキスト要素（見出し・4象限カード本文・軸ラベル・下部帯・footer）を1要素ずつ確認。切れ・重なり・はみ出しなし |
| 自己完結-外部参照なし | PASS | CDN・Webフォント・外部画像・fetch の参照なし。図は inline SVG（warning アイコン）のみ |
| 文字コード-charset明記 | PASS | 先頭に `<meta charset="utf-8">` |
| 配色-ビジネス基調 | PASS | 白面+ネイビー基調。緑（--good）=最優先の推奨・読み順チップ、赤（--critical）=警告帯のみに限定。グレー（--ink-3/--border）=非優先の抑制表現 |
| 比率-スライド16対9 | PASS | 1280×720 実描画で崩れなし |
| 内部名称-混入なし | PASS | 内部スキル名・内部パスの表示なし |
| 寸法-固定1280x720 | PASS | `.slide` の width/height が 1280px/720px 固定 |
| 単位-固定px限定 | PASS | `grep -nE 'vw|vh|clamp\(' 解説スライド.html` で `.slide` 内該当なし（スケーリング script 内の `innerWidth/innerHeight` のみ、これは JS 側で `.slide` CSS ではない） |
| フッタ-区切り線 | PASS | `.slide-meta` は区切り線+下端余白のみで内容は空 |
| 具体性-実値明示 | PASS | 「変更頻度はバージョン管理の履歴（git log）から機械的に集計できる」の実在ツール名（git log）を明示。出所: 依頼元の確定内容（タスク指示文）にそのまま準拠。特定行数（例: 250万行）等の未検証値は使わず「数百万行規模」の一般表現に統一 |
| 効果数値-未検証記載禁止 | PASS | 削減率・短縮時間等の効果数値は一切記載していない |
| wbr-文節改行 | 対象外 | 見出し・帯テキストに `<wbr>` を使用していない（1行に収まる長さで設計したため分割不要と判断） |
| 因果-構造表現 | 対象外 | 本スライドは2軸マトリクス型であり、時系列の変化トリガー（イベント線・バッジ）を要する構造ではない |
| 識別子-実物想起 | 対象外 | 実在ツール・製品のロゴ表現を含まない |
| 状態-極端表現 | 対象外 | メーター・消費表現を持たない |
| 時間-軸割当 | 対象外 | 横軸は「変更頻度」、縦軸は「事業重要度」であり時間軸ではない |
| 時間スケール-図分離 | 対象外 | 時間スケールを扱わない図のため該当なし |
| 例示数値-検算整合 | 対象外 | 図中に検算を要する具体数値（%・件数等）を配置していない |
| 読み順-番号対応 | PASS | 図中のチップ ①（x軸ラベル横）②③（右上「最優先」カード内）と、footer `.steps` のチップ ①②③ で番号・色（緑=`--good`）が一致 |
| 軸範囲-業務文脈 | 対象外 | 業務時間軸を扱わない図のため該当なし |

## 4象限の軸整合性の自己検査（タスク限定観点）

- 横軸（変更頻度）: 左=低 → 右=高。縦軸（事業重要度）: 下=低 → 上=高
- 右上（高頻度×高重要度）="最優先" ラベル・緑の強調枠 — 指示どおり
- 右下（高頻度×低重要度）="2番手" — 指示どおり
- 左上（低頻度×高重要度）="3番手" — 指示どおり
- 左下（低頻度×低重要度）="整備しない"・グレー表示（`opacity:0.72` + 破線枠） — 指示どおり
- 軸の向きと象限優先順位ラベルの対応関係に矛盾なし（自己検査 PASS）

## 自己検査結果サマリ

- PASS: 12件 / 対象外: 8件 / NG: 0件
- document-reviewer 独立判定（1回目）: FAIL（:root 外 hex 直書き 2 箇所 — `.step-chip` の `color: #fff`、`.quad-card--top .quad-tag` の `color: #fff`）→ `:root` に `--on-accent: #FFFFFF;` を追加し両箇所を `color: var(--on-accent);` に置換して修正済み。修正後は `:root` 外の hex 直書き 0 件（awk による `:root` ブロック除外 grep で確認）。スクリーンショット再取得・目視でチップ番号「3番手」「2番手」と「最優先」タグの文字が白のまま、文字切れなしを確認
