# 検査記録: 読み取り専用先行導入-権限段階解禁

## 観点レビュー表の照合結果（2026-07-19）

目視根拠: `evidence/検証用スクリーンショット.png`（1280×720、Playwright headless Chromium で実描画）

| 観点キー | 判定 | 備考 |
|---|---|---|
| 文字切れ-全数目視 | PASS | スクリーンショットで全テキスト要素（3段階カードの見出し・3行リスト・帯・フッタ）に切れ・重なり・はみ出しなし |
| 自己完結-外部参照なし | PASS | CDN・外部フォント・外部画像・fetch なし。図は inline SVG（矢印2本）のみ |
| 文字コード-charset明記 | PASS | 先頭行に `<meta charset="utf-8">` |
| 配色-ビジネス基調 | PASS | 白面+ネイビー基調。緑=読み取り専用の安全性・低リスク、赤=両極失敗の警告、灰=中間段階に限定。彩度の高い色の乱用なし |
| 比率-スライド16対9 | PASS | 1280×720 実描画で崩れなし |
| 内部名称-混入なし | PASS | consult- 等の内部スキル名・内部パスの表示なし |
| 寸法-固定1280x720 | PASS | `.slide { width:1280px; height:720px; }` 固定 |
| 単位-固定px限定 | PASS | `grep -nE 'vw\|vh\|clamp\(' file` で該当は `.slide-viewport`（100vw/100vh、テンプレート標準の外枠）のみ。`.slide` 内に vw/vh/clamp() なし |
| フッタ-区切り線 | PASS | `.slide-meta` は区切り線+下端余白のみで内容なし |
| 具体性-実値明示 | PASS | 帯下部に実在ツール名「Claude Code」を明記し、権限設定（permission）による強制という実装事実を記載。出所: 本タスクの確定内容（brain 起案・ユーザー確認済みの導入方式説明） |
| 効果数値-未検証記載禁止 | PASS | 工数・時間・削減率等の数値記載なし |
| wbr-文節改行 | 対象外 | 実描画で全テキストが自然折返しにより文字切れ・重なりを起こしておらず、`<wbr>` による強制改行制御を要しない |

## 図解スライド固有の観点（段階比較型）

| 観点キー | 判定 | 備考 |
|---|---|---|
| 因果-構造表現 | 対象外 | 本スライドは時系列上の引き金（上限到達等）を説明する図ではなく、3段階の並び+矢印による段階比較構造のため、イベント線・バッジは不要 |
| 識別子-実物想起 | 対象外 | 実在ツールのロゴ描画は行っていない（帯テキスト内でツール名「Claude Code」を文字列として言及するのみ） |
| 状態-極端表現 | 対象外 | メーター・消費状態の表現を持たない構造のため対象外 |
| 時間-軸割当 | 対象外 | 時間経過を表す図ではなく、段階（成熟度）の比較図のため横軸は時間ではない |
| 時間スケール-図分離 | 対象外 | 時間軸パネルを持たないため対象外 |
| 例示数値-検算整合 | 対象外 | 図中に検算を要する具体数値（時刻・割合等）を含まない |
| 読み順-番号対応 | PASS | 段階カードの step-chip（1=緑/2=灰/3=灰）とフッタ steps の step-chip が同番号・同色で対応 |
| 軸範囲-業務文脈 | 対象外 | 時間軸を持たないため対象外 |

## 自己検査サマリー

- 判定対象 20 観点中、PASS 12・対象外 8・NG 0
- 修正反復回数: 0（初回描画で崩れ・切れが無かったため再取得なし）

document-reviewer による独立判定: 後工程で追記

## document-reviewer 独立判定（1回目）追記（2026-07-19）

- 判定: FAIL（:root 外 hex 直書き 2 箇所 — `.step-chip { color: #fff; }` / `.risk-tag--accent { color: #fff; }`）
- 対応: 両箇所を `:root` 既存の `--surface: #FFFFFF` を使い `color: var(--surface);` へ置換して修正済み
- 修正後確認: `:root` ブロック外の hex 直書きは awk による除外 grep で 0 件。`evidence/検証用スクリーンショット.png` を再取得し、チップ番号(1/2/3)とリスクタグ「管理下」の文字が白のまま・文字切れなしを目視確認
