# 検査記録: 特性テスト-変更前安全網

## 観点レビュー表の照合結果（2026-07-19）

実描画: `evidence/検証用スクリーンショット.png`（1280×720 PNG、目視確認済み）

| 観点キー | 判定 | 備考 |
|---|---|---|
| 文字切れ-全数目視 | PASS | 1280×720 実描画で全テキスト要素（ヘッダ・用語定義帯・4枚のステージカード・警告帯・フッタ3項目）を確認。切れ・重なり・はみ出しなし |
| 自己完結-外部参照なし | PASS | CDN・Webフォント・外部画像・fetch の参照なし |
| 文字コード-charset明記 | PASS | 先頭に `<meta charset="utf-8">` |
| 配色-ビジネス基調 | PASS | 白面+ネイビー基調。赤(critical)=注意帯のみ・緑(good)=STEP4到達段階のみ・コーラル(claude)=STEP3のAI変更段階のみに限定 |
| 比率-スライド16対9 | PASS | 1280×720 実描画で崩れなし |
| 内部名称-混入なし | PASS | 内部スキル名・内部パスの表示なし |
| 寸法-固定1280x720 | PASS | `.slide { width: 1280px; height: 720px; }` 固定 |
| 単位-固定px限定 | PASS | `grep -nE 'vw\|vh\|clamp\('` で `.slide` 内該当なし（`100vw`/`100vh` は `.slide-viewport` のみ） |
| フッタ-区切り線 | PASS | `.slide-meta` は区切り線+空要素のみ |
| 具体性-実値明示 | PASS | 各ステップの work 項目に「本番ログ」「手動実行」「assert」「仕様書」等の具体手段を明記。出所: `generating-explanation-html-slides` の先行スライド（AI駆動開発導入-五段階計画表・実害検証-完了定義）で確立済みの特性テスト運用パターンを踏襲し、一般的なソフトウェアテスト実務（特性テスト/characterization test の標準的定義）から導出。プロジェクト固有の未検証数値は含めない |
| 効果数値-未検証記載禁止 | PASS | 時間短縮・削減率等の効果数値は一切記載していない |
| wbr-文節改行 | PASS | ファイル内に `<wbr>` を使用していない（`grep -c wbr` = 0）ため、check-wbr-quality.sh の wbr-存在性検査は対象外（`total_wbr>0` 時のみ発火する仕様）。実測: `bash check-wbr-quality.sh <file>` で exit 0（エラー・警告なし）。先行スライド（実害検証-完了定義）と同様の運用 |
| 因果-構造表現 | PASS | 4ステップのフロー矢印（stage-arrow）+ 警告帯（layer-band--critical）で「特性テストは正しさを保証しない」という限界を構造部品で明示 |
| 読み順-番号対応 | PASS | group-row のチップ①②③（partial/main/good）とフッタ steps のチップ①②③が同色・同意味で一致。STEP1・2は①(partial)、STEP3は②(main)、STEP4は③(good) |
| 識別子-実物想起 / 状態-極端表現 / 時間-軸割当 / 時間スケール-図分離 / 例示数値-検算整合 / 軸範囲-業務文脈 | 対象外 | 実在ツール描画・消費状態・時間軸表現が本テーマ（工程フロー）に存在しないため対象外 |

## 自己検査サマリー

- 全 14 観点中 PASS 14 件、対象外 6 項目（型の性質上非該当）、NG 0 件
- 修正反復: 0 回（初回描画で全観点 PASS）

## タスク限定の観点（更新時の再検査項目）

- 「特性テストは正しさを保証しない」という注意（既存不具合もそのまま固定される旨）が warning 帯から欠落しないこと
- 4ステップの順序（記録する → 固定する → 変更する → 育てる）を入れ替えない。順序自体が「安全網を先に張ってからAIに書き込みを許す」という主張の骨格
- 用語定義帯（特性テストの1行定義）をヘッダ直下の目立つ位置から動かさない

## document-reviewer による独立判定

- 独立判定（1回目）: FAIL（`.step-chip` に `:root` トークン経由でない hex 直書き `color: #fff` が1箇所存在）
- 対応: `.step-chip { color: #fff; }` を `color: var(--surface);`（`--surface: #FFFFFF`）に置換し、`:root` 外の hex 直書きを解消。置換後、`:root` ブロック外に hex 値が残存しないことを確認済み（残存数 0）
- 再検証: スクリーンショットを再取得し、チップ番号(①②③)の文字色が白のまま・文字切れなしを目視確認済み
