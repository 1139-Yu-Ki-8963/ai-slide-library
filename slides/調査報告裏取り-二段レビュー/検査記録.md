# 検査記録: 調査報告裏取り-二段レビュー

## 観点レビュー表の照合結果（2026-07-16）

| 観点キー | 判定 | 備考 |
|---|---|---|
| 文字切れ-全数目視 | PASS | 1280×720 実描画で全テキスト要素を確認（修正 2 回目で溢れ解消を再確認） |
| 自己完結-外部参照なし | PASS | 外部 URL 参照なし |
| 文字コード-charset明記 | PASS | 先頭に meta charset="utf-8" |
| 配色-ビジネス基調 | PASS | 白面+ネイビー基調。緑=最終段（判定と裏取り）に限定 |
| 比率-スライド16対9 | PASS | 1280×720 で崩れなし |
| 内部名称-混入なし | PASS | 内部スキル名・内部パスなし |
| 寸法-固定1280x720 | PASS | .slide 固定寸法 |
| 単位-固定px限定 | PASS | .slide 内に vw/vh/clamp なし |
| フッタ-区切り線 | PASS | .slide-meta は空 |
| 因果-構造表現 | PASS | 三段パイプラインを layer-band + 層間注記で構造表現。差し戻しは文で明記 |
| 読み順-番号対応 | PASS | 層のチップ 1/2/3 が工程順と一致 |
| 識別子-実物想起 / 状態-極端表現 / 時間-軸割当 / 時間スケール-図分離 / 例示数値-検算整合 / 軸範囲-業務文脈 | 対象外 | 実在ツール描画・消費状態・時間軸が本テーマに存在しない（「上限 2 回」は運用規約値で検算対象外） |

business-content-reviewer 照合: 初回 FAIL（誇大表現「必ず」・失敗時/負荷懸念の先回りなし・行動接続なし）→ 修正 → 再照合 PASS

反復記録:
1. 初回内容レビュー FAIL 3 件 → 「必ず」削除・差し戻し上限 2 回の明記・「導入の最初の一歩」追加
2. 追加行でレイアウトが縦に溢れ → layer-band padding 12px・gap 10px に圧縮し「導入の最初の一歩」を出典行へ統合 → 再描画で崩れなし
3. 2026-07-17 具体性不足の指摘（ユーザーレビュー）を受け、チェックリスト必須の hook 停止・調査役の権限固定・裏取りの実例（再集計）を追記

## タスク限定の観点（更新時の再検査項目）

- 三段（チェックリスト作成 → 調査 → 判定と裏取り）の主体（依頼元 / 調査役 / 判定役）を混同しない
- 「差し戻しは上限 2 回」の運用値は実際の運用規約と一致させる
- 層 3 の説明文が長いため、文言追加時は縦溢れを実描画で再確認する
