# 検査記録: リバース工程-基本設計の完了判定

## 出所

見出し・副題・左の6項目・右の縦フロー図の文言と構造は、ユーザー提供の設計データ `kihon_reverce.html`（独自デザインシステム参照のため非公開）をそのまま書き起こしたもの。文言の追加・省略・言い換えは行っていない。色は共有シェルのCSS変数（`var(--ink)` `var(--muted)` `var(--label)` `var(--faint)` `var(--accent)` `var(--surface)` `var(--surface-2)`）へ置き換え、元データが独自トークン（`var(--fill-1)`）を使っていた「工程」ボックスの背景は`var(--surface-2)`へ、白文字のハードコード（`#FFFFFF`）は`var(--surface)`へ置き換えた。右の不合格ループの折れ線は、元データのCSS境界線トリックからinline SVGへ置き換えた（視覚的な形状は同一：完了判定側から工程側へ戻る右向き矢印を持つ折れ線）。

## 観点レビュー表の照合結果（2026-09-06）

### 共通観点

| 観点キー | 判定 | 備考 |
|---|---|---|
| 自己完結-外部参照なし | PASS | 外部参照は共有シェル（../../assets/shared-slide-shell.css / .js）のみ |
| 文字コード-charset明記 | PASS | `<meta charset="utf-8">` あり |
| 配色-用途とブランド適合 | PASS | 共有シェルのトークンのみ使用。hexの直書きなし |
| 寸法-固定1280x720 | PASS | 共有シェルの`.slide`（1280×720固定）を使用 |
| 単位-固定px限定 | PASS | 型固有CSSはpxのみ。vw/vh/clamp不使用（SVGのviewBoxは単位無しの座標） |
| 内部名称-混入なし | PASS | 内部スキル名・実行環境の絶対パスの表示なし |
| 効果数値-未検証記載禁止 | PASS | 効果・工数の数値記載なし |
| 具体性-実値明示 | PASS | 6項目は元データの具体的な条件文をそのまま使用 |

### 図解スライド固有の観点

| 観点キー | 判定 | 備考 |
|---|---|---|
| 因果-構造表現 | PASS | 「不合格→名指しの項目だけ直す」という戻りの向きをSVGの矢印で明示 |
| 読み順-番号対応 | PASS | 左の項目番号1〜6と、右の完了判定「左の6項目をすべて満たしているか」が対応 |
| 主張-単一 | PASS | タイトル・副題で「合格の6条件」という単一主張を宣言 |
| 行粒度-同一 | PASS | 左の6行はすべて「規約が定める条件」という同種の行 |

## 反復記録

1. 初版で作成し、`npm test`全チェックPASSを確認。

## タスク限定の観点（更新時の再検査項目）

- 左の6項目の文言は元データ（`kihon_reverce.html`）の条件文と一致させること。要約・言い換えをしない
- 「工程」「完了判定」「次の工程」の3ボックスと、不合格時に戻るSVGの矢印の向き（完了判定側から工程側への戻り）を維持すること
- 件名（title）を共通名称「AI駆動開発 支援ツール — 既存コードから設計書を起こす工程：〜」に統一（ユーザー指示）
